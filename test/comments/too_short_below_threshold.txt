This is a deliberately short comment with far fewer than one hundred words.
It only exists to verify the CLI's local validation path, so it must stay short.
The moderator should reject this instantly without any network call at all.
